from setuptools import setup, find_packages

setup(
    name="dsa",  # Name of your package
    version="0.1",
    packages=find_packages(),  # Automatically find sub-packages
    install_requires=[],  # Add dependencies here if needed
)